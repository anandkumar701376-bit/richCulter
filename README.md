# Culter

Project scaffold for the Culter cultural data platform.

## Structure

- `backend/` FastAPI service and database layer
- `frontend/` web client
- `database/` seed data and backups
- `docs/` project documentation
- `scripts/` data and database utilities
