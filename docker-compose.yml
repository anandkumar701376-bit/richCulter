services:

  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      -./storage/media:/app/storage/media

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"